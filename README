rock-bsp
===========

Getting Started
---------------

Attention : before make, you should choose the target board

1. Choose a board doing `./config.sh board`, or `./config.sh` to see
   the list of supported boards.

2. Run 'make' to build and pack nand/emmc/sdcard image or 'make help' to list available targets
